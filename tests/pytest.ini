# pytest.ini
[pytest]
testpaths = tests
norecursedirs = *.egg-info .git .venv venv __pycache__ dist build *.zip

# Add verbosity and output formatting
addopts = -ra -q --tb=short

# Show logs in real-time during test runs
log_cli = true
