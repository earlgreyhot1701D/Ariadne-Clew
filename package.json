{
  "name": "ariadne-frontend",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint . --ext .js",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0"
  }
}
